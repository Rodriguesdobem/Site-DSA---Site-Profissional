services:
  - type: web
    name: tenda-do-ozi-backend
    runtime: java
    plan: free
    buildCommand: ./mvnw clean package -DskipTests
    startCommand: java -jar target/backend-0.0.1-SNAPSHOT.jar
    envVars:
      - key: CORS_ALLOWED_ORIGINS
        value: https://SEU-FRONTEND.vercel.app
